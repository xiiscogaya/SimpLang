package compiler.taulasimbols;

// Descripción para subprogramas
class DProc extends Descripcio {
    int np; // Identificador único del subprograma

    DProc(int np) {
        super("dproc");
        this.np = np;
    }
}