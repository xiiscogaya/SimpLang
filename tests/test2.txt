# PROGRAMA 2 - FUNCIONA
# Realiza la suma con una función auxiliar y imprime el resultado por pantalla

def int suma(int a, int b) {
    return a + b;
}

{
    int b;

    # Numero base del que se empieza a sumar
    int resultado;
    input(resultado);

    repeat {
        input(b);
        resultado = suma(resultado, b);
    } until resultado < 200;

    print(resultado);
}